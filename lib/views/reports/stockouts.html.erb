<h2><%= t('reports.index.stockouts_by_date') %></h2>

<%= render :partial => 'graphs', 
          :locals => { :interactive_parameters => [], 
                        :tables => @tables, 
                        :graphs => @graphs, 
                        :tab_graphs => @tab_graphs, 
                        :tab_graph_dimension => @tab_graph_dimension } %>

